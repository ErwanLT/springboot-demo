# Tutoriel Upload/Download de Fichiers

API REST pour téléverser et télécharger des fichiers.

## Endpoints

- `POST /uploadFile` (multipart `file`)
- `POST /uploadMultipleFiles` (multipart `files[]`)
- `GET /downloadFile/{fileName}`

## Comment l'exécuter

```bash
mvn spring-boot:run
```
