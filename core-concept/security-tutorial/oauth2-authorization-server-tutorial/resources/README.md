# Resource Server OAuth2

API protégée (resource server) utilisant des JWT émis par l'authorization server.

## Endpoints

- `GET /articles`

## Configuration clé

- Port : `8090`
- Issuer : `http://localhost:9000`

## Comment l'exécuter

```bash
mvn spring-boot:run
```
